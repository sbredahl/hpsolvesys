This is the HP48S/SX "version" of SOLVESYS, which was a stack solver (no GUI in HP48S/SX) and was named stksolve.lib.

The code here reproduces the S/SX compatible stksolve.lib library that was released together with SOLVESYS 3.1 (for the G/GX).








